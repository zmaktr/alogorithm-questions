# sets is build in data type 
# sets are mutable but cannot contain mutable types such as lists, dict & sets
# sets cannot contain duplicate item 
# sets are not ordered by sequence

dict = {4,'d'}
sets = {'a':'A','b':'B'}
dict.add('e')
sets['a'] = 'AA'

#print(dict)
#print(type(dict))
#print(sets)
